# Install packages to images docker, "common" is declared into dependencies.list of alpine6-node-base.
add="inotify-tools"

# Delete packages to images docker, "common" is declared into dependencies.list of alpine6-node-base.
del=""
