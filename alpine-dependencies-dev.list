# Install packages to images docker, "common" is declared into dependencies-extra of alpine6-node-base.
add="inotify-tools"

# Delete packages to images docker, "common" is declared into dependencies-extra of alpine6-node-base.
del=""
